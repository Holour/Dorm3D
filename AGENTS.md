# AGENTS.md

## 项目概述
本项目旨在根据已有图片和尺寸信息，为北京邮电大学学生十公寓的宿舍空间进行3D建模。

## 项目结构

每次新增或调整目录时，需要同步更新本节。

```text
Dorm3D/
├── AGENTS.md              # 项目协作要求与目录结构
├── README.md              # 项目说明与本地构建运行方式
├── TODO.md                # 当前任务列表
├── index.html             # 本地交互模型界面
├── package.json           # 依赖与启动、构建、测试命令
├── package-lock.json      # 锁定依赖版本
├── vite.config.js         # 本机服务与打包配置
├── 启动模型.cmd            # Windows 一键启动
├── .env.example           # 本地配置模板
├── data/                  # 可查询实测尺寸目录
├── src/                   # 交互、尺寸搜索、自由移动、坐姿控制与界面样式
│   └── model/             # 布局、房间、家具与材质生成
├── tests/                 # 实测几何、布局、尺寸搜索、移动/坐姿与导出验证
├── docs/                  # 尺寸记录、照片依据、确认记录与项目进展
├── exports/               # 已导出的 GLB 模型、模型预览及界面截图
├── pictures/              # 原始宿舍照片（仅本地保留，不提交）
├── dist/                  # 本地构建产物（自动生成，不提交）
└── node_modules/          # 本地依赖（自动生成，不提交）
```

## 技术栈

你自主选择决定，要求保证建模效果的同时低门槛方便运行。

当前实现：Three.js + Vite + 原生 JavaScript；本地浏览器交互，支持 GLB 导出，Node.js 20.19+。不依赖外部素材 CDN 或云服务。

## 重要约定
- 自主维护和检查 `TODO.md` 中未完成的任务，自主规划完成顺序，每完成一条及时更新`TODO.md` 中的任务列表从`- [ ] `到 `- [x]`，当 `TODO.md` 中的任务全部完成后，将其清空。
- 宿舍内部图片在 `pictures/` 目录下。
- 对于每次任务，你需要查看和维护`docs/项目进展.md`，在其中说明已经完成的部分。
- 环境变量在 .env.local 中，参考 .env.example。
- 遇到有多种实现方案时，列出选项让我选择，而不是直接选一种。
- 注意查看和维护`README.md`，在其中更新和说明项目构建与运行方式。
- 当前为开发阶段，所有工作在本地完成。
- 每次新增或调整功能时，先检查项目中是否已有同类实现，避免冗余。
- 寝室空间尺寸可能存在误差较大处，若你发现有相关联的尺寸之间违背空间关系，请找我二次确定。 

## 禁止事项
- 禁止主观臆断我的意图，对我交给你的任务有不理解或与现状有冲突的地方都应该问我。
- 禁止主观猜测寝室空间布局，务必以参考图片和尺寸信息为准。
