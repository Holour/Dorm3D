# 用户确认记录

## 2026-09-10 第一轮

- 室内净长 660cm **不包含**阳台 90cm；层高采用 265cm。
- 站在入口面向阳台，左手第一张为 1 号，顺时针编号。因此从入口向阳台：左列 1、2、3；右列 6、5、4。
- 两端床架距对应端墙均约 30cm。
- 两侧桌背离墙各约 5cm；桌背靠墙存在小挡板，目测厚约 2cm、高约 7cm，仅用于建模参考，**不要在模型内标记挡板尺寸**。
- 柜体原记录为内部尺寸，需考虑板材厚度以及柜体可能未贴墙。
- 桌面长 91cm **不包括**侧边书柜。
- 书柜“长 55cm”沿桌深方向，即垂直墙面；“深 26cm”沿床长方向。

这些是用户补充的空间约束，不覆盖 `寝室空间尺寸.md` 的原始记录。柜板厚度与精确离墙距离仍未作为实测值给出。

## 2026-09-10 第二轮

- 床长 190cm 为**内长**，外部护栏和架子另占空间。
- 铁架宽度采用用户给出的估计值 2.5cm。
- 共用梯子的床相连接，中间只有一个护栏。
- 不共用梯子的床彼此断开，中间有分别属于两张床的两个铁架，并留微小缝隙。
- 由三段 190cm 内长、共梯组合三处端框和独立床两处端框计算，纵向总长约 582.5cm，另加独立组合间微小缝隙；若在 660cm 内居中，则端部净空约 38cm。该值与初次“约30cm”不同，已请求用户确定尺寸优先级。

## 仍待确认

- 下列历史待确认项已在第四轮全部解决，不再需要用户重复确认。
- 纵向优先级已在第三轮确认（见下），具体额外结构缺少尺寸，作为占位余量，不补造构件。
- 如采用各侧桌背离墙约 5cm，桌间净宽计算为 330 − 2 × (58 + 5) = 204cm，与约 200cm 的原记录相差 4cm；保留测量误差说明，不悄悄缩放桌深。
- 梯柜距离依赖柜体外深及柜后间隙，应使用确认后的外廓关系核验。

## 2026-09-10 第三轮

- 用户要求**保留两端各约 30cm**，不采用端部余量均分为约 38cm 的方案。
- 用户说明多出来的部分可能来自床的其他结构。因此模型应同时保留 190cm 床内长、2.5cm 铁架参考宽度、两端约 30cm 的布局优先级；600cm 纵向床组占位内，其余未明确结构作为余量管理，不凭空增造具体杆件。
- 接下来仍需确认交付方式，以及未测门窗、设备、柜板等细节的近似建模范围。

## 2026-09-10 第四轮：交付、测量查询与陈设

- 用户选择本地浏览器交互模型，支持点击查询、旋转、剖切和 GLB 导出。
- 允许柜板、门窗、空调、暖气、椅子等未完整测量细节按照片近似建模。
- 所有用户原始测量记录应能点击对应部位查询；估计值只参与建模，不显示估计尺寸，不混入实测目录。
- 椅子按正常使用状态放在各自桌前；照片中放在床上是打扫时的临时状态，不在模型中照搬。

至此，本轮所需决策已经确认，以上较早阶段的“待确认”作为过程记录保留。
