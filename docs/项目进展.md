# 项目进展

## 2026-09-10：坐姿抽屉互动

- 已按补充要求增加坐姿抽屉互动：点击本床抽屉拉出，再次点击收回，复用键盘架的平移动画；抽拉行程不作为实测值展示。
- 抽屉顶板固定，箱体及把手活动，拉出后上方开口；23×38×12cm内腔和三条实测标线随箱体保持正确。
- 32组测试及生产构建通过。浏览器实际验证1号、6号床抽屉往返抽拉、其他床位不联动、坐姿眼点不移动，未发现运行错误。
- 已更新GLB与预览图，GLB校验0错误0警告，45项实测记录完整保留；TODO按约定清空。

## 2026-09-10：桌柜朝向、尺寸查询、坐姿与移动端

- 已按用户确认统一六组桌柜正面：衣柜在左、侧边书柜及抽屉在右；柜内层架左、挂衣区右。床号、床架和梯子归属保持确认后的关系。
- 点击实体直接显示实测标注；搜索支持中文格号和床号，例如“三号床 衣柜第二格”。点选记录自动聚焦具体柜格，临时隐藏周围遮挡，并可返回完整空间。
- 已增加方向键/WASD连续平移，按当前水平视线前后左右移动；输入框、弹窗和失焦时不会误移动。
- 每床新增椅上固定眼点，坐姿中按键或鼠标/单指拖动只转头；禁用平移和缩放，F聚焦不会改变座位位置，退出后恢复自由查看。
- 坐姿可点击本床键盘架拉出/收回、点击衣柜开关柜门。键盘架的外滑轨固定，活动板与内滑轨同步抽拉；眼点及行程不进入实测目录。
- 手机界面新增“尺寸查询 / 视角与操作”切换，默认展示尺寸；坐姿扩大模型画面、提供紧凑四向触摸按钮，进入座位或定位尺寸时自动显示模型区域。
- 31组自动检查通过。独立浏览器已验证尺寸定位、直接点选、自由移动、固定坐姿、家具点击、导出和390px手机布局，未发现运行错误。
- GLB及模型预览已重新导出；GLB校验0错误、0警告，6床6椅4梯和45项实测记录完整保留。
- 手机实际触摸事件验证通过，单指拖动不误触家具，双指手势不改变坐姿位置；本轮任务完成，TODO按约定清空。

## 2026-09-10：公开 GitHub 仓库

- 用户授权建立公开仓库并推送项目，明确排除 `pictures/`。
- 已确认 GitHub 账号为 `Holour`，目标仓库为 `Holour/Dorm3D`。
- 已将 `pictures/` 加入 Git 忽略规则；包含照片打包副本的 `dist/`、本地依赖和 `.env.local` 同样不提交。
- README 已补充公开副本运行方式及本地照片的可选使用说明；照片入口按实际文件数显示，无照片时给出明确提示。
- 按待提交文件建立独立副本，确认不存在 `pictures/`，重新安装锁定依赖后通过全部10组测试、GLB校验（0错误、0警告）和生产构建；该构建仅生成HTML、CSS及JS，没有照片资源。
- 已创建并推送至公开仓库 [Holour/Dorm3D](https://github.com/Holour/Dorm3D)，默认分支为 `main`，本地 `origin` 已关联。
- 已通过 GitHub API 核对仓库为 `PUBLIC`，远程31个文件不包含 `pictures/`、`dist/`、`node_modules/` 或 `.env.local`；本地9张原始照片完整保留。生成模型的GLB与预览图随仓库提供。
- 本次发布的是项目仓库，模型仍按 README 在本地运行；完成后清空 TODO。

## 2026-09-10：首版交互式宿舍模型

### 已完成

- 检查所有项目资料；修正另一项目遗留的 README，建立本项目运行说明。
- 逐张查看 9 张原始照片，整理照片证据、原始尺寸、用户确认记录。
- 根据确认结果建立六人寝室：室内不含阳台，左列1/2/3、右列6/5/4；共梯1/2及4/5，独立梯3及6；两端各约30cm。
- 还原床板、框架、护栏、四梯、衣柜分区及开门、桌面、键盘架、抽屉、机柜、上下书架和正常放在桌前的六把椅子。
- 用户授权照片近似细节后，加入门窗、空调、暖气、灯具、插座和窗帘。
- 搭建 Three.js + Vite 本地查看器：旋转、缩放、平移、四类观察视角、床位定位、点击部件查询、柜门开合、上铺显隐、水平剖切、照片对照、GLB及PNG导出。
- 所有可显示尺寸统一来自45项原始测量目录，估计参数不进入界面或导出测量属性。
- 几何测试10组通过，覆盖实际板件、内腔、分区、床位朝向、梯子归属、椅子位置和完整数据挂接。
- 浏览器逐类核查45项记录，直接点击桌面及剖切操作通过；桌面与390px窄屏布局检查通过。
- 完整GLB已生成，Khronos glTF Validator检查0错误、0警告，全部45个实测ID保留。
- 增加Windows启动入口、依赖锁、配置模板、构建与导出验证命令。

### 最终校准

- 细分尺寸标线已绑定到具体床板、护栏和柜内分层的实际边界。六组镜像床位共252条线段与原始长度匹配，另18处公区或范围记录使用位置锚点。
- 新增回归检查：床板厚度端点、两类护栏高度、衣柜四格各自位置，以及包含踏步伸出的110cm梯间净宽和38–40cm梯柜距离；10组测试全部通过。
- 已验证生产构建中的床板5cm与衣柜第二分区30cm标注，运行无错误；整体模型、查询、导出和本地启动均已完成，TODO按项目要求清空。

### 保留的依据边界

- 原始尺寸文件保持不变，用户补充和优先级单独记录。
- 190cm是床内长；额外床架结构占位保留在用户要求的两端间距内，不擅自添加无法从照片识别的构件。
- 衣柜内尺寸和柜板、安装间隙分别处理，不通过缩放内腔来凑外廓。
- 公区手测记录按原值查询；未实测外观使用照片近似，估计值不展示。
- 项目只在本机运行，没有进行线上部署。

详细检查见《验收记录.md》。

